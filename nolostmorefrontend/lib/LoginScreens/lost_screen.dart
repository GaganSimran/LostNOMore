// lost_screen.dart
import 'package:flutter/material.dart';

class LostScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Report Lost Item")),
      body: const Center(child: Text("Lost Item Screen")),
    );
  }
}

